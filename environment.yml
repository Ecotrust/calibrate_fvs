name: calibrate_fvs

channels:
  - conda-forge

dependencies:
  - geopandas 
  - ipywidgets
  - jinja2
  - jupyterlab
  - matplotlib
  - openpyxl
  - pandas
  - pip
  - pip:
      - -e .  # install local refit_fvs package in editable mode
  - psycopg2
  - python
  - seaborn
  - sqlalchemy
  - tqdm
  - xlrd

prefix: /users/ddiaz/mambaforge/calibrate_fvs
